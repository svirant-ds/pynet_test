#!/usr/bin/env python
list = ['this','is','almost','five','strings']
list.append('one')
list.append('more')
list[1]
list.pop(0)
lg = len(list)
print(f"the list is {lg} long\n")
list.sort()
print(list)
